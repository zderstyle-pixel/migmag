# Appsmith self-hosted configuration
# Copy this file to .env and fill in the values

# Generate strong random values for these (e.g. openssl rand -hex 32)
APPSMITH_ENCRYPTION_PASSWORD=changeme
APPSMITH_ENCRYPTION_SALT=changeme

# Supervisor web UI password (accessible at /supervisor)
APPSMITH_SUPERVISOR_PASSWORD=changeme
